# Claude SDK adapter

Use `@anthropic-ai/claude-agent-sdk` for programmatic Claude Code capabilities, or `@anthropic-ai/claude-code` for the CLI. Shared prewarm should not invoke Claude Code file/bash tools. User Box continuation can invoke the SDK/CLI normally because the Box is the user's isolated machine.
