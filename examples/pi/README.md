# Pi / PI adapter

Pi appears on npm as `@earendil-works/pi-coding-agent` (current) and `@mariozechner/pi-coding-agent` (older/alternate). Both are coding agent CLI packages with read/bash/edit/write tool support and session management. Use Box handoff to isolate those full tools to each user's own Box.
