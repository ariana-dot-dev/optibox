# Hermes adapter

Hermes Agent is a Nous Research open-source agent CLI for coding/research/development. Provider docs show `hermes chat --provider hf`; npm also has an unofficial `hermes-agent` bridge. This adapter keeps all Hermes tool execution on the user's Box.
