# OpenClaude adapter

OpenClaude is available as `@gitlawb/openclaude` and positions itself as an open coding-agent workflow for many local/cloud model providers. This adapter demonstrates handoff into OpenClaude on a per-user Box.
